{
  "name": "pocket-atlas",
  "version": "0.2.0",
  "private": true,
  "description": "Pocket Atlas — an interactive LEGO-style travel globe",
  "type": "module",
  "scripts": {
    "test": "node --test scripts/*.test.mjs",
    "start": "python3 -m http.server 4173 --bind 127.0.0.1 --directory src",
    "dev": "npm start",
    "build": "node scripts/build.mjs",
    "preview": "python3 -m http.server 4173 --bind 127.0.0.1 --directory dist"
  },
  "engines": {
    "node": ">=18"
  }
}
